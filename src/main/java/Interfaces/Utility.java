package Interfaces;

public class Utility {
    // This method doesn't care about implementation
    // It only cares that it should methods of Stack

    // Abstraction
    public static void someUtilityMethod(Stack stack) {
        stack.push(5);
        stack.pop();
    }
}
